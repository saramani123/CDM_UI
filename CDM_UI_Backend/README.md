# CDM UI Backend

FastAPI service for the CDM platform. **Setup, ports, env vars, and one-off scripts are documented in the [repository root README](../README.md).**

Quick reference:

```bash
pip install -r requirements.txt
cp env.example .env
python main.py
```

- API: `http://localhost:10000` — OpenAPI at `/docs`, health at `/health`.
